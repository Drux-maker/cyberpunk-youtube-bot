# ─── Dependencias para modo local (sin APIs de pago) ─────────────────────────
# Instalar DESPUÉS de requirements.txt base

# ─── MusicGen (Meta AudioCraft) ───────────────────────────────────────────────
# Requiere PyTorch con CUDA primero (ver setup.md)
audiocraft

# ─── Stable Diffusion (HuggingFace Diffusers) ─────────────────────────────────
diffusers>=0.30.0
transformers>=4.44.0
accelerate>=0.33.0
safetensors>=0.4.4

# ─── xformers — reducción de VRAM (~30% menos) ───────────────────────────────
# Instalar con: pip install xformers --index-url https://download.pytorch.org/whl/cu121
xformers

# ─── Audio ────────────────────────────────────────────────────────────────────
torchaudio
